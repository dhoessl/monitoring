---
- name: 'Create persistent folders'
  file:
    path: '{{ monitoring_docker_host_dir }}/{{ item }}'
    state: 'directory'
  loop:
    - 'grafana/data'
    - 'grafana/conf'
    - 'influxdb/data'
    - 'influxdb/conf'
  tags:
    - 'monitoring:filesystem'
    - 'monitoring:init'
...
